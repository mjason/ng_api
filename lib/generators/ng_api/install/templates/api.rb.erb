require "helpers"

module <%= app_name %>

  class API < Grape::API
    prefix "api"
    format :json
    formatter :json, Grape::Formatter::Jbuilder

    helpers Helpers

    resource :demo do
      get "/", :jbuilder => "demo" do
        # @demo = models_object
        # eg: @demo = User.first
        @demo = {name: "bestng" } # delete line
      end
    end
  end

end